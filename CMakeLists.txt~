cmake_minimum_required(VERSION 2.8)
PROJECT(hiermrf)

#if you don't want the full compiler output, remove the following line
SET(CMAKE_VERBOSE_MAKEFILE OFF)

ADD_DEFINITIONS(-std=gnu++0x)

FIND_PACKAGE(ITK REQUIRED)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

INCLUDE_DIRECTORIES (/home/collab/xhzhu/Libraries/armadillo-2.4.4/include)
INCLUDE_DIRECTORIES (${PROJECT_SOURCE_DIR})

#LINK_DIRECTORIES (/home/sci/weiliu/packages/boost_1_46_1/build/lib)
#LINK_DIRECTORIES (/home/sci/weiliu/packages/lemon_build/lemon)



ADD_EXECUTABLE(HardiToolbox
  main.cpp HardiToolbox.cpp utils.cpp
  )

#need to link to some other libraries ? just add them here

TARGET_LINK_LIBRARIES(HardiToolbox ${ITK_LIBRARIES})
#TARGET_LINK_LIBRARIES(hiermrf emon ${ITK_LIBRARIES} boost_program_options boost_filesystem)
#TARGET_LINK_LIBRARIES(singlemrf emon ${ITK_LIBRARIES} boost_program_options boost_filesystem)
